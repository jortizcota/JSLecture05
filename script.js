// 1.Define a variable “myGreeting” and place a string containing a generic greeting inside of it.

let myGreeting = "Hello User!";

// 2.Output the contents of “myGreeting” using console.log(...).

console.log(myGreeting); // Hello User!

// 3.Define a constant variable “pi” and place a number containing the value for Pi inside of it.

const pi = 3.14;

// 4.Attempt to re-assign “pi” to something else - what happens? Why?

// pi = 1; Error since pi is a constant is cna only be assigned to one number.

// 5.Define a new variable “myNumber” and place a number of your choice inside of it.

let myNumber = 9;

// 6.Call the “min” function inside of the “Math” object on both “myNumber” and “pi” - what is the result? Why?

console.log(Math.min(myNumber, pi)); // 3.14 because the Math.min statement pulls the lowest number from the numbers provided.

//Practice

const myEquation1 = 9 * 3 + 24; // 51

const myEquation2 = 24 - 12 * 2 + 15; // 39

console.log(Math.max(myEquation1, myEquation2, 52, 73, 13)); // 73

console.log(myEquation1 - myEquation2); // 36

console.log(myEquation1 + myEquation1 - myEquation2 * myEquation1); // -663

